{% extends 'shared/layout/email/email_with_image.html.twig' %}

{% block content %}
    <div class="content_login">
        <div class="line">
            <div class="margin-bottom">
                <div class="margin">
                    <article class="s-12 l-8 center">
                        <h2>Mise à jour de votre annonce !</h2>
                        <p class="subtitile_presentation">Louez facilement votre appartement sur le site.</p>
                        <p class="margin-bottom-home">Votre annonce {{ appartment.title }} à {{ appartment.city.name }} a changé de statut.</p>
                        <p class="margin-bottom-home">{{ status }}</p>
                        <a href="{{ app.request.getSchemeAndHttpHost() ~  path('app_announcement_listing', {'type' : 'appartement'}) }}"><button class="btn btn-primary btn_result">Voir les annonces</button></a>
                    </article>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
